use std::collections::HashMap;

use lvl_math::{Quat, Vec3};


pub struct ResetBoneSet {
    // bones: HashMap<String, >
}

pub struct BoneBindTransform {
    pub position: Vec3,
    pub rotation: Quat
}